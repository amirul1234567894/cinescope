'use client'

/**
 * AdUnit Component — DISABLED (Auto Ads handles everything)
 *
 * This component is kept for future use but renders nothing.
 * Google Auto Ads (loaded via AdSenseScript in layout.tsx) handles all
 * ad placements automatically using AI-optimized positions.
 *
 * To enable manual ad slots in the future:
 * 1. Get specific ad slot ID from AdSense dashboard
 * 2. Replace the contents of this component with the slot rendering code
 */
export function AdUnit() {
  return null
}
